export const metadata = {
    title: 'Login | HotelApp',
}

const Layout = ({ children }: any) => { 
    return (
        <div>
            <div>
                {children}
            </div>
        </div>
    )
}

export default Layout